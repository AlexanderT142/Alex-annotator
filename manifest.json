{
  "id": "local-pdf-annotator",
  "name": "Local PDF Annotator",
  "version": "0.1.0",
  "minAppVersion": "1.4.0",
  "description": "Read PDFs and add persistent text-highlight annotations stored in a local sidecar file. Bundles its own pinned pdf.js + worker (loaded via a runtime Blob URL) so it can never collide with Obsidian's internal pdf.js worker.",
  "author": "Alexander",
  "isDesktopOnly": true
}
